"use strict";
const {CONST: CONST} = await import(`/js/module/socket/constant.js${ver_string}`);
export const FUNCTION = {
    // 아래 영역에 default에서 사용한 함수 중 새로 적용할 함수 복사하여 내용 수정
    ///////////////////////////////////////////////////////////////////////////////////////////////////////////////
    ///////////////////////////////////////////////////////////////////////////////////////////////////////////////
};